# /etc/modprobe.d/99-hp-c640-power.conf
# HP Pro c640 (Intel Comet Lake-U) Graphics and Wireless Power Tuning
#
# NOTE (kernel >= 7.0): the iwlwifi "d0i3_disable" parameter was REMOVED
# upstream and the shipped config no longer contains it — re-adding it would
# make modprobe fail and WiFi would not load at all.
# Also note enable_guc=2 means "HuC load only" on 7.0 (GuC submission is
# already auto-enabled via enable_guc=-1 default).

# i915: Disable PSR (Panel Self Refresh) to eliminate suspend/resume black screen freezes,
# while enabling FBC (Framebuffer Compression) and GuC submission for low power consumption.
options i915 enable_psr=0 enable_fbc=1 enable_guc=2

# iwlwifi: Enable power saving while maintaining low latency (D0i3).
# NOTE: "d0i3_disable" was REMOVED upstream in kernel 7.0; do not re-add it
#       (modprobe would fail and WiFi would not load at all).
options iwlwifi power_save=1 uapsd_disable=0
